# EMR Development Guide

대상 독자: EMR 도메인 경험이 부족한 개발팀  
기준 소스: `Data/UBerSoft.EMR.Data`  
목적: 현재 EMR의 핵심 업무 흐름을 이해하고, 피부/성형외과 EMR 개발 범위와 순서를 잡기 위한 가이드

## 1. 이 문서의 관점

현재 소스는 C# 기반이지만, 새 시스템의 개발 언어가 다르다면 클래스나 메서드를 그대로 옮기는 것은 핵심이 아니다. 중요한 것은 EMR 업무 데이터가 어떤 순서로 확정되고, 각 단계에서 어떤 도메인 규칙이 적용되는지 이해하는 것이다.

따라서 개발팀은 다음 순서로 이해해야 한다.

```text
환자
-> 보험/자격
-> 접수
-> 진료
-> 상병
-> 처방/시술/재료
-> 금액 계산
-> 수납
-> 청구/리포트/통계
```

피부/성형외과는 기존 보험청구 중심 EMR과 달리 비급여 시술, 패키지, 선결제, 상담, 사진, 동의서, 예약/CRM 비중이 크다. 다만 기본 골격은 같다. 환자가 오고, 접수되고, 진료/시술이 기록되고, 금액이 계산되고, 수납되며, 필요하면 보험청구 또는 증빙 문서로 이어진다.

## 2. 개발팀이 먼저 알아야 할 핵심 원칙

### 업무 흐름과 화면 흐름은 다르다

화면은 병원 운영 방식에 따라 달라질 수 있다. 하지만 데이터 확정 순서는 비교적 고정적이다.

- 환자가 없으면 접수를 만들 수 없다.
- 접수가 없으면 대기/진료 흐름을 안정적으로 만들기 어렵다.
- 진료가 없으면 상병, 처방, 시술, 재료를 귀속시킬 수 없다.
- 처방/시술/재료가 없으면 금액 계산이 불가능하다.
- 금액 계산이 확정되어야 수납과 청구가 가능하다.

### 금액 계산은 화면 로직이 아니다

진료비, 본인부담, 할인, 지원금, 청구액 계산은 반드시 도메인 서비스로 분리해야 한다. 화면마다 계산식을 복사하면 보험 규칙, 비급여 정책, 패키지 정책이 바뀔 때 오류가 퍼진다.

### 기준 데이터와 발생 데이터는 분리한다

기준 데이터는 약품, 수가, 시술, 재료, 상병, 특정기호, 용법, 병원 설정 같은 마스터이다. 발생 데이터는 실제 환자에게 적용된 진료, 상병, 처방, 수납, 청구이다.

처방 마스터의 가격이 바뀌어도 과거 진료 금액이 바뀌면 안 된다. 따라서 발생 데이터에는 당시 적용된 단가, 급여 여부, 수량, 일수, 의사, 면허번호 같은 값을 스냅샷으로 남겨야 한다.

### 날짜 기준 정책이 중요하다

의료 도메인은 진료일 기준으로 단가, 급여 여부, 본인부담률, 산정특례, 병원 종별가산, 보험 자격이 달라진다.

정책은 단순 현재값이 아니라 적용일 기준으로 선택되어야 한다.

```text
정책 선택 기준 = 환자 + 진료일 + 병원 + 보험유형 + 처방/상병/특정기호
```

## 3. 전체 개발 범위

### 1차 범위: EMR 기본 운영

피부/성형외과에서 먼저 필요한 최소 운영 범위이다.

- 병원/사용자/권한 기본 설정
- 환자 등록 및 조회
- 예약 또는 접수
- 진료기록 작성
- 상병 입력
- 처방/시술/재료 입력
- 비급여 가격 계산
- 수납
- 영수증/진료내역 출력
- 기본 매출 통계

이 단계에서는 보험청구 전체를 완성하기보다, 진료와 수납 데이터가 안정적으로 쌓이는 것이 더 중요하다.

### 2차 범위: 급여 진료와 계산 고도화

보험진료를 병행하는 피부과라면 필요한 범위이다.

- 보험자 유형과 자격 정보
- 급여/비급여 처방 구분
- 진료일 기준 수가/약가/재료대 적용
- 본인부담 계산
- 산정특례/차상위/의료급여 일부 대응
- 청구 전 검증

피부/성형외과에서는 모든 보험청구 규칙을 처음부터 구현하지 않아도 된다. 실제 병원이 다루는 보험 범위를 먼저 정해야 한다.

### 3차 범위: 보험청구/SAM/고급 통계

보험청구를 실제 제출해야 할 때 필요한 범위이다.

- 청구서/명세서 생성
- 동일 수진자 명세서 병합
- 특정내역 자동 생성
- 추가청구/누락청구/보완청구
- SAM 파일 생성
- HIRA 청구 프로그램 연동
- 접수/반송/심사결과 수신
- 삭감/반송 대응 통계

이 영역은 규칙이 많고 외부 프로그램 의존성이 있어 후순위로 두는 것이 안전하다.

### 4차 범위: 피부/성형 특화

기존 EMR 소스보다 새 시스템에서 더 중요해질 가능성이 높은 범위이다.

- 상담 관리
- 시술 패키지
- 선결제/예치금
- 시술권 차감
- 예약/리마인드
- 시술 사진
- 동의서/전자서명
- 멤버십/쿠폰/프로모션
- CRM 재방문 관리
- 직원별 매출/시술 실적

이 영역은 기존 보험 중심 로직을 그대로 따르기보다 새 도메인 모델을 설계해야 한다.

## 4. 추천 개발 순서

### 1단계: 기준 코드와 설정

먼저 병원이 운영에 필요한 기준 데이터를 관리할 수 있어야 한다.

상세 기준: `Base_Code_Domain.md`

필요 데이터:

- 병원 정보
- 사용자/의사/직원
- 진료실/부서
- 상병
- 처방/수가
- 시술/상품
- 재료
- 결제수단
- 할인/프로모션 정책

개발 포인트:

- 기준 데이터는 삭제보다 사용 여부를 제어하는 방식이 안전하다.
- 가격은 적용일 이력을 가져야 한다.
- 보험 수가와 비급여 시술 가격은 같은 처방 입력 UI에서 쓰일 수 있지만, 가격 정책은 분리되어야 한다.

### 2단계: 환자와 자격 정보

환자는 모든 업무의 시작점이다.

필요 데이터:

- 환자 기본정보
- 연락처
- 주민등록번호 또는 생년월일/성별
- 보험 정보
- 차상위/의료급여/산정특례 등 자격 정보
- 특이사항, 알레르기, 메모

개발 포인트:

- 환자 기본정보와 보험 자격은 분리한다.
- 보험 자격은 진료일 기준으로 조회되어야 한다.
- 피부/성형에서는 개인정보와 사진/동의서가 강하게 연결되므로 접근 권한과 로그가 중요하다.

### 3단계: 접수와 대기

접수는 환자를 특정 날짜의 진료 흐름에 올리는 행위이다.

기본 흐름:

```text
환자 선택
-> 진료일 선택
-> 진료실/담당자/의사 선택
-> 보험 또는 비급여 내원 구분
-> 대기 상태 생성
```

개발 포인트:

- 접수 상태와 진료 상태를 분리한다.
- 예약 환자가 내원하면 예약이 접수로 전환된다.
- 접수 없이 진료를 직접 생성할 수 있게 할지 정책을 정해야 한다.

### 4단계: 진료 작성

진료는 상병, 처방, 시술, 메모, 의사, 진료일을 묶는 중심 데이터이다.

기본 흐름:

```text
접수 선택
-> 진료 생성
-> 주상병 입력
-> 처방/시술/재료 입력
-> 진료 메모 입력
-> 금액 계산
-> 저장
```

개발 포인트:

- 진료 저장 시점에 금액을 재계산해야 한다.
- 진료일이 바뀌면 보험 자격, 가격, 급여 여부가 다시 평가되어야 한다.
- 진료와 수납은 분리하되, 진료 저장 후 수납 대기 데이터가 만들어져야 한다.

### 5단계: 상병 로직

상병은 보험청구와 진료기록의 근거가 된다.

기본 흐름:

```text
상병 마스터 선택
-> 환자 성별/나이 제한 확인
-> 불완전 코드 여부 확인
-> 주상병 가능 여부 확인
-> 진료과/진료결과/면허번호 기본 설정
-> 보험 자격 기준 산정특례 특정기호 자동 적용
```

개발 포인트:

- 주상병은 최소 1개가 있어야 한다.
- 주상병으로 쓸 수 없는 상병은 차단해야 한다.
- 성별/나이 제한은 차단 또는 경고 정책을 정해야 한다.
- 산정특례 특정기호는 환자 보험 자격과 상병을 함께 보고 자동 세팅한다.

참고 소스:

- `Services/Concrete/DiseaseToTreatmentService.cs`

### 6단계: 처방/시술/재료 로직

처방 입력은 EMR에서 금액과 청구를 결정하는 가장 중요한 발생 데이터이다.

기본 흐름:

```text
마스터 선택
-> 약품/수가/재료/시술/묶음 유형 판별
-> 진료일 기준 가격과 급여 여부 확인
-> 환자 보험 유형에 따라 급여/비급여 결정
-> 기본 수량/횟수/일수 세팅
-> 원내/원외 구분
-> 의사 면허번호 또는 예외코드 세팅
-> 라인 금액 계산
```

개발 포인트:

- 보험 환자라도 해당 진료일에 급여 적용 불가인 항목은 비급여로 전환되어야 한다.
- 일반 환자는 기본적으로 비급여 처리된다.
- 진료 행위에는 의사 면허번호가 필요하다.
- 원내 주사제처럼 자동 예외코드가 필요한 항목이 있다.
- 묶음 처방은 parent-child 관계를 가져야 한다.
- 피부/성형의 시술 패키지는 기존 Bundle 개념을 참고하되, 선결제/차감과 연결할 수 있도록 별도 설계가 필요하다.

참고 소스:

- `Services/Concrete/PrescribeToTreatmentService.cs`
- `Services/Concrete/TreatmentUnionService.cs`

### 7단계: 금액 계산

금액 계산은 처방 라인 금액, 보험 본인부담, 지원금, 청구액, 수납 예정액을 만드는 단계이다.

기본 흐름:

```text
처방 라인 금액 계산
-> 종별가산 대상 분리
-> 요양급여비용총액1 계산
-> 100/100 및 100/100 미만 항목 분리
-> 보험 유형별 본인부담 계산
-> 산정특례 적용
-> 장애인의료비/지원금 계산
-> 청구액 계산
-> 요양급여비용총액2 계산
```

개발 포인트:

- 금액 계산은 항상 진료일 기준이어야 한다.
- 본인부담 계산은 보험 유형별 전략으로 분리한다.
- 산정특례는 기본 계산과 별도로 계산한 뒤 최종 선택한다.
- 절사 단위는 정책별로 다르므로 공통 유틸로 처리한다.
- 피부/성형 비급여 중심이라면 초기에는 보험청구 항목보다 비급여 가격, 할인, 패키지, 선결제 차감을 먼저 안정화한다.

참고 문서:

- `Docs/Patient_Cost_Domain.md`

참고 소스:

- `Services/Computes/Concrete/AmountService.cs`
- `Services/Computes/Concrete/PatientCostService*.cs`

### 8단계: 수납

수납은 진료비 계산 결과를 실제 결제 데이터로 확정하는 단계이다.

기본 흐름:

```text
수납 대기 조회
-> 진료비 계산 결과 확인
-> 할인/조정/지원금 반영
-> 결제수단별 금액 입력
-> 수납 확정
-> 영수증/증빙 출력
-> 일마감/월마감 반영
```

개발 포인트:

- 진료비 계산 결과와 실제 결제 금액은 다를 수 있다.
- 할인, 미수, 환불, 선결제 차감은 수납 도메인에서 관리해야 한다.
- 카드/현금/계좌/포인트/예치금 등 결제수단별 분리가 필요하다.
- 피부/성형에서는 선결제와 시술권 차감이 핵심이다.

### 9단계: 보험청구

보험청구는 진료와 수납 이후의 별도 업무이다.

기본 흐름:

```text
청구 대상 진료 조회
-> 청구 전 검증
-> 건강보험/의료급여 분리
-> 명세서 생성
-> 동일 수진자 명세서 병합
-> 청구서 금액 합산
-> SAM 파일 생성
-> 암호화/점검/송신
-> 수신 결과 반영
```

개발 포인트:

- 청구는 수납과 분리해야 한다.
- 명세서 병합은 단순 합산이 아니라 상병, 처방 줄번호, 특정내역, 금액 재계산이 필요하다.
- 추가청구는 기존 청구분과 비교해 차감 후 남은 항목만 청구한다.
- 외부 HIRA 프로그램 연동은 운영 환경 의존성이 크므로 후순위로 둔다.

참고 소스:

- `Services/InsuranceCharges/Concrete/BillService.cs`
- `Services/InsuranceCharges/Concrete/InsuranceChargeValidater.cs`
- `Services/InsuranceCharges/Concrete/SAMs/Sends/SAMSendService.cs`

## 5. 현재 소스의 도메인 구성 방식

현재 Data 프로젝트는 다음 역할로 나뉜다.

### Controller

화면 또는 상위 모듈이 사용하는 업무 진입점이다. ViewModel을 다루고 Repository와 Service를 조합한다.

예:

- 환자 controller
- 처방 controller
- 청구 controller
- 리포트 controller

### Repository

서버 API와 통신해 데이터를 조회/저장한다. 업무 규칙보다 데이터 접근이 중심이다.

예:

- `TreatmentRepository`
- `PatientRepository`
- `BillRepository`

### Service

실제 도메인 규칙이 가장 많이 들어 있는 곳이다.

예:

- 금액 계산
- 본인부담 계산
- 처방 변환
- 상병 변환
- 청구서 생성
- SAM 파일 생성

### Adapter

서로 다른 화면/명세서 모델을 같은 계산 인터페이스로 맞춘다.

예:

- 진료 데이터와 청구 명세서 데이터를 모두 `IAmountSource`로 변환해 같은 계산 로직을 사용한다.

## 6. 새 시스템 구현 방법

### 도메인 서비스를 먼저 설계한다

화면을 먼저 만들면 계산과 업무 규칙이 화면 안에 섞인다. 다음 서비스는 초기에 분리해야 한다.

- `PatientService`
- `ReceptionService`
- `TreatmentService`
- `DiseaseService`
- `OrderService` 또는 `PrescribeService`
- `PricingService`
- `PatientCostService`
- `PaymentService`
- `ClaimService`

### 정책은 전략으로 분리한다

보험 유형, 나이, 특정기호, 적용일에 따라 달라지는 계산은 if문 하나에 계속 추가하면 유지보수가 어렵다.

권장 구조:

```text
PatientCostCalculator
-> InsurancePatientCostPolicy
-> Over65PatientCostPolicy
-> MedicalAidPatientCostPolicy
-> NoInsurancePatientCostPolicy
-> SpecialCasePolicy
```

### 금액 계산 결과는 명시적인 객체로 반환한다

단순 숫자 하나만 반환하면 나중에 어떤 항목이 어떻게 계산됐는지 추적할 수 없다.

필요 결과:

- 총 진료비
- 급여 금액
- 비급여 금액
- 본인부담금
- 청구액
- 지원금
- 할인
- 수납 예정액
- 계산에 사용된 정책
- 절사 전/후 금액

### 발생 데이터에는 계산 근거를 남긴다

운영 중 금액 문의, 환불, 청구 오류, 삭감 대응이 발생한다. 따라서 계산 결과만 저장하지 말고 근거도 남겨야 한다.

예:

- 진료일
- 적용 단가
- 수량/횟수/일수
- 급여 여부
- 본인부담 코드
- 산정특례 코드
- 사용된 계산 정책
- 할인 사유
- 수납 조정 사유

### 보험청구는 후순위로 격리한다

보험청구는 핵심이지만, 초기에 모든 기능을 묶어 만들면 개발 속도가 크게 떨어진다.

먼저 안정화할 것:

- 진료 데이터
- 처방/시술 데이터
- 금액 계산
- 수납

그 다음 청구:

- 청구 대상 조회
- 청구 전 검증
- 명세서 생성
- SAM 파일
- 수신 결과

## 7. 피부/성형외과에서 별도로 결정해야 할 것

### 보험 중심인가, 비급여 중심인가

피부과는 보험진료와 비급여 시술이 섞일 수 있고, 성형외과는 비급여 비중이 더 클 수 있다.

초기에 다음을 정해야 한다.

- 보험청구가 1차 오픈 범위인지
- 비급여 시술이 중심인지
- 같은 진료에서 보험 처방과 비급여 시술을 같이 수납할지
- 상담과 시술을 진료로 볼지 별도 업무로 볼지

### 패키지와 선결제

피부/성형에서는 일반 EMR의 묶음 처방보다 복잡한 결제 구조가 필요할 수 있다.

필요 개념:

- 패키지 상품
- 회차권
- 선결제
- 예치금
- 부분 사용
- 환불
- 양도/소멸
- 이벤트 가격
- 직원별 성과 배분

기존 `Bundle`은 처방 묶음 관점이다. 피부/성형의 패키지는 결제와 회차 차감까지 포함하므로 별도 도메인으로 설계하는 것이 안전하다.

### 사진/동의서/상담

피부/성형에서는 진료기록 외에 사진, 상담 기록, 동의서가 중요하다.

설계 시 분리:

- 진료기록
- 상담기록
- 시술기록
- 사진
- 동의서
- 전자서명

사진과 동의서는 개인정보 민감도가 높으므로 접근 권한, 열람 로그, 삭제 정책이 필요하다.

## 8. 개발 리스크가 큰 영역

### 금액 계산

리스크:

- 잘못 계산되면 환자 수납, 병원 매출, 보험청구가 모두 틀어진다.

대응:

- 계산 서비스를 독립 테스트 가능하게 만든다.
- 대표 케이스를 테스트 데이터로 고정한다.
- 절사, 할인, 지원금, 선결제 차감을 분리한다.

### 보험청구

리스크:

- 명세서 오류는 반송, 삭감, 심사불능으로 이어진다.

대응:

- 청구 전 검증을 별도 단계로 둔다.
- SAM 생성은 독립 모듈로 둔다.
- 청구 데이터는 진료/수납 데이터와 상태를 분리한다.

### 기준 데이터

리스크:

- 가격과 급여 여부가 바뀌면 과거 진료 재계산 문제가 발생한다.

대응:

- 적용일 이력을 둔다.
- 발생 데이터에 당시 적용값을 복사한다.
- 마스터 삭제 대신 비활성 처리한다.

### 패키지/선결제

리스크:

- 환불, 부분 사용, 회차 차감, 직원 성과 배분이 뒤늦게 추가되면 구조 변경이 크다.

대응:

- 결제, 사용, 환불을 각각 별도 이력으로 남긴다.
- 패키지 상품과 실제 시술 기록을 분리한다.

## 9. 우선 만들 문서

현재 문서 이후에는 아래 순서로 구체화하는 것이 좋다.

1. `EMR_Workflow_Guide.md`: 환자 접수부터 수납까지 업무 흐름 상세.
2. `Treatment_Order_Domain.md`: 상병, 처방, 시술, 묶음 로직 상세.
3. `Payment_Domain.md`: 수납, 할인, 환불, 선결제, 회차권 설계.
4. `Insurance_Claim_Domain.md`: 청구서, 명세서, 검증, SAM 상세.
5. `Plastic_Dermatology_Extensions.md`: 피부/성형 특화 도메인 설계.

## 10. 현재 참고 문서

- `Docs/EMR_Data_Domain_Map.md`: 현재 Data 프로젝트의 도메인 지도.
- `Docs/Patient_Cost_Domain.md`: 본인부담/진료비 계산 상세.

이 가이드는 개발 범위와 순서를 잡기 위한 상위 문서이고, 세부 도메인 문서는 이 문서를 기준으로 하나씩 확장한다.
